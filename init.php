<?php

use Illuminate\Support\Facades\Event;

// Event::listen('auth.login', function($event) { 
//     dd('EVENTOOO');die();

//  }, 10);

//  Event::listen('mailer.send', function ($message, $view, $data) {
//     \Log::info("$view has had content added to the message", $data, $message);
// });